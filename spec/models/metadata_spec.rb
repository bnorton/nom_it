describe "metadata" do
  describe "new" do
    it "should create a new metadata reecord" do
    end
    it "should that record should have all the keys we need" do
    end
    it "should have all the values at zero" do
    end
    it "should " do
    end
  end
  describe "updates" do
    it "should increment on metric on a single item" do
    end
    it "should increment multimple metrics on an item" do
    end
    it "should increment multiple metrics on multiple items" do
    end
  end
  describe "fetches" do
    it "should get the metadata from a new item (all zeroes)" do
    end
    it "should have a single value for single incerements" do
    end
    it "should " do
    end
  end
end